(function() {
  describe('MachineStock', function() {
    it('removes 1 french roast', function() {
      var stock;
      stock = new MachineStock();
      return expect(stock.purchaseFrenchRoast()).toBe(true);
    });
    return it('removes 1 french roast', function() {
      var stock;
      stock = new MachineStock();
      return expect(stock.purchaseFrenchRoast()).toBe(true);
    });
  });

}).call(this);
